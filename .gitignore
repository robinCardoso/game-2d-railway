# Dependências
node_modules/

# Build
dist/

# Segredos e ambiente local (manter .env.example versionado)
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo

# SO
.DS_Store
Thumbs.db
desktop.ini

# IDE / editor (opcional local)
.vscode/
.idea/
*.swp
*.swo
*~

# Dados locais de volume (Railway usa /data no servidor)
/data/

# Cobertura de testes
coverage/

# Electron build output
release/
release-build-*/
release/win-unpacked-stale-*/
.electron-output-dir

# Chaves ECDSA do empacotamento (geradas localmente ou via CI secret)
private_key.pem
public_key.pem

# Artefatos gerados por `npm run pack` (recriados no build)
public/assets.pak
public/assets.sig
public/public_key.pem
